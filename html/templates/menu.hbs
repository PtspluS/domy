<!DOCTYPE>

<html>

    <head>
        <title>{{name}} - Menu</title>
    </head>

    <body>

        <h1>{{name}}</h1>

        <ul>
            {{#each menu_list}}
            <li>
                {{name}} {{price}} <br/>
                {{info}}
            </li>
            {{/each}}
        </ul>

    </body>

</html>